import { type loginProps, schemaLoginForm } from "./loginSchema";
import { type registerProps, schemaRegisterForm } from "./registerSchema";

export {
  schemaLoginForm,
  schemaRegisterForm,
  type loginProps,
  type registerProps,
};
